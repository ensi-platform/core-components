const FormError = () =>
    // const {
    //     formState: { errors },
    // } = useFormContext();

    // useError({
    //     message: Object.keys(errors).length > 0 ? 'Форма заполнена некорректно' : '',
    //     code: '',
    //     status: 400,
    //     name: 'Ошибка формы',
    // });

    null;
export default FormError;
