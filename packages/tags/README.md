Tags component is a flex-wrapper for list of nameplates (TagItem elements).  
It can be used to show some list of small nameplates and allow to remove items from it. For example as inside multiselect input.
